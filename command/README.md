# Connecting to Server and starting server

## Connecting to remote server
`$ ssh -i <path-to-your-key-pair> ubuntu@<dns-name-of-your-server>`

## Starting up server
Run in server terminal
`$  node index.js`